'use client';\n\nimport { ReactNode } from 'react';\nimport Link from 'next/link';\nimport { cn } from '@/lib/utils';\n\ninterface ButtonProps {\n  children: ReactNode;\n  variant?: 'primary' | 'secondary' | 'outline' | 'ghost';\n  size?: 'sm' | 'md' | 'lg';\n  className?: string;\n  href?: string;\n  [key: string]: any;\n}\n\nexport default function Button({\n  children,\n  variant = 'primary',\n  size = 'md',\n  className,\n  href,\n  ...props\n}: ButtonProps) {\n  const baseStyles = 'inline-flex items-center justify-center rounded-lg font-semibold transition-all duration-300 focus:outline-none focus:ring-2 focus:ring-offset-2';\n\n  const variants = {\n    primary: 'bg-montessori-sage text-white hover:bg-montessori-olive focus:ring-montessori-sage',\n    secondary: 'bg-montessori-beige text-montessori-olive hover:bg-montessori-cream focus:ring-montessori-beige',\n    outline: 'border-2 border-montessori-sage text-montessori-sage hover:bg-montessori-sage hover:text-white focus:ring-montessori-sage',\n    ghost: 'text-montessori-sage hover:bg-montessori-cream focus:ring-montessori-sage',\n  };\n\n  const sizes = {\n    sm: 'px-4 py-2 text-sm',\n    md: 'px-6 py-3 text-base',\n    lg: 'px-8 py-4 text-lg',\n  };\n\n  const buttonClass = cn(\n    baseStyles,\n    variants[variant],\n    sizes[size],\n    className\n  );\n\n  if (href) {\n    return (\n      <Link href={href} className={buttonClass}>\n        {children}\n      </Link>\n    );\n  }\n\n  return (\n    <button className={buttonClass} {...props}>\n      {children}\n    </button>\n  );\n}\n