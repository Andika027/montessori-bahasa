'use client';\n\nimport { ReactNode } from 'react';\nimport { cn } from '@/lib/utils';\n\ninterface BadgeProps {\n  children: ReactNode;\n  variant?: 'sage' | 'olive' | 'terracotta' | 'brown';\n  className?: string;\n}\n\nexport default function Badge({ children, variant = 'sage', className }: BadgeProps) {\n  const variants = {\n    sage: 'bg-montessori-sage text-white',\n    olive: 'bg-montessori-olive text-white',\n    terracotta: 'bg-montessori-terracotta text-white',\n    brown: 'bg-montessori-brown text-white',\n  };\n\n  return (\n    <span className={cn('inline-block px-3 py-1 rounded-full text-sm font-semibold', variants[variant], className)}>\n      {children}\n    </span>\n  );\n}\n